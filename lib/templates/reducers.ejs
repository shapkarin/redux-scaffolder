import {
  <%= constants.join(',\n  ') %>
} from './constants';

const initialState = {};

export default function<%= name ? ` ${name}` : '' %>(state = initialState, action) {
  switch (action.type) {
    <% for(constant of constants){ %>
    case <%= constant %>:
      return {
        ...state
      };
    <% }%>
    default:
      return state;
  }
}