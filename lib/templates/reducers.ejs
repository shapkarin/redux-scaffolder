import {
  <% for (const constant of constants) {  %> 
    <%= constant %>,
  <% } %>
} from './constants';
<%= blank %>
const initialState = {};
<%= blank %>
export default function <%= name %>(state = initialState, action) {
  switch (action.type) {
    <% for (const constant of constants) {  %> 
      case <%= constant %>:
        return {
          ...state
        };
    <% } %>
    default:
      return state;
  }
}
