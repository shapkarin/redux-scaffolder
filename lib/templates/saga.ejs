import { fork, <%= effects.join(', ') > <%= take %> } from 'redux-saga/effects';
<%= blank %>
import {
  <%= blank %>
} from './actions';
<%= blank %>
import { <%= constant %> } from './constants';
<%= blank %>
function* myWorker(action) {
  <%= _try ? 'try {' : '' %>
    <% if(effects.length){ %>
      <% for(const effect in effects){%>      
        yield <%= effect%>()
      <% } %>
    <% } %>
  <% if(_try){ %>
    } catch(e) {
      <%= blank %>
    }
  <% } %>
}
<%= blank %>
export default function* () {
  yield <%= take %>(<%= constant %>, myWorker);
}