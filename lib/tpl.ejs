import { Component } from 'react';
<% const cleanedProptypes = Object.keys(proptypes).join(', ').replace(/\*/g, ''); %>

<% if (hasProptypes === 'yes') { %>
  import PropTypes from 'prop-types';
<% } %>

class <%= name %> extends Component {
  <% if (hasDefaultProps === "yes"){%>
    static defaultProps = {
       <% for (var prop in proptypes) {  %>  
      <% } %>
        open: false
      }
  <% } %>

  render (){
    <%# todo: new lines %>
    const { <%= cleanedProptypes %> } = this.props;
    return (
      null
    )
  }
}

<% if (hasProptypes === 'yes'){ %>
  Some.propTypes = { 
    <% for (var prop in proptypes) {  %> 
      <% var [name, required] = prop.match(/^[a-z0-9]+|\*$/gi); %>
      <%= name %>: PropType.<%= proptypes[prop] %><%= required ? '.isRequired' : '' %>,
    <% } %>
  }
<% } %>
