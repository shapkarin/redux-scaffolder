import { Component } from 'react';
<% const cleanedProptypes = Object.keys(proptypes).join(', ').replace(/\*/g, ''); %>
<%= blank %>
<% if (hasProptypes === 'yes') { %>
  import PropTypes from 'prop-types';
<% } %>
<%= blank %>
class <%= name %> extends Component {
  <% if (hasProptypes === 'yes'){ %>
    static propTypes = {
      <% for (var prop in proptypes) {  %> 
        <% var [name, required] = prop.match(/^[a-z0-9]+|\*$/gi); %>
        <%= name %>: PropTypes.<%= proptypes[prop] %><%= required ? '.isRequired' : '' %>,
      <% } %>
    }
  <%= blank %>
  <% } %>
  <% if (hasDefaultProps === "yes"){%>
    static defaultProps = {
       <% for (var prop in proptypes) {  %>  
      <% } %>
        open: false
      }
  <% } %>
  <%= blank %>
  render (){
    <%# todo: new lines %>
    const { <%= cleanedProptypes %> } = this.props;
    <%= blank%>
    return (
      null
    )
  }
}
<%= blank %>


